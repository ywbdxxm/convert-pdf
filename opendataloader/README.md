# OpenDataLoader Workspace (archived)

> **Status:** archived 2026-04-18. Not part of the active pipeline.

This overlay existed when the project was comparing `docling_bundle` against
`OpenDataLoader PDF` (hybrid mode). Per project rule 3 in
[开发要求.md](../开发要求.md), only `docling_bundle` is developed now.

The `.venv/` and `requirements.txt` here are kept so old experiments can be
reproduced if needed; no new code targets this path.

- Active pipeline: see the root [README.md](../README.md) and
  [docs/architecture.md](../docs/architecture.md).
- Decision record: see `task_plan.md` under `Decisions Made`.
