# Global Workstation Reference

The canonical cross-project workstation reference for this machine is:

- `/home/qcgg/.codex/docs/ai-workstation-architecture.md`

Use that document for:

- environment layering decisions
- `micromamba` shared AI base policy
- overlay environment setup
- mirror, cache, and activation rules
- known pitfalls already encountered on this workstation

This repository may add project-specific details, but it should not silently contradict the global workstation reference.
