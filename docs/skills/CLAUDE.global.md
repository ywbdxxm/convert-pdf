# Global CLAUDE.md (user-level)

## Embedded / chip-manual tasks

When the user asks about **ESP32 / ESP32-S3 / ESP32-C3 / MCU datasheets, TRMs, registers, pins, GPIO mux, peripherals, electrical characteristics, firmware / HAL / driver code** — or anything that references specific chip identifiers, pins, or register names:

**Invoke the `using-manual-bundles` skill before doing anything else.**

The skill has the full consumption flow, absolute paths, jq patterns, citation format, and fallback rules. Do not improvise — load the skill.
